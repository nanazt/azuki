# Auto-generated by gaji
# Do not edit manually - Edit workflows/docker.ts instead
# Generated at: 2026-09-14T03:48:21Z

jobs:
  build:
    env:
      FALLBACK_ID: azuki-kache-linux-amd64-v1-kache-0.20.0-fallback-${{ github.run_id }}-${{ github.run_attempt }}
      RESTORE_ID: azuki-kache-linux-amd64-v1-kache-0.20.0-restore-${{ github.run_id }}-${{ github.run_attempt }}
    permissions:
      actions: read
      contents: read
      packages: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - id: buildx
      name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    - uses: docker/login-action@v3
      with:
        password: ${{ secrets.GITHUB_TOKEN }}
        registry: ghcr.io
        username: ${{ github.actor }}
    - continue-on-error: true
      env:
        REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        REGISTRY_USERNAME: ${{ github.actor }}
      id: restore
      name: Restore kache snapshot
      run: |-
        set -euo pipefail
        if ! node scripts/docker-cache.mjs restore \
          --cache-id "${{ env.RESTORE_ID }}" \
          --builder "${{ steps.buildx.outputs.name }}" \
          --cache-target /var/cache/kache \
          --transfer-image docker.io/library/debian:trixie-slim@sha256:abc9cb88a5587630d7f915f47b23b0668fe250fbfc6457aa4d52b534c1bbf73f \
          --registry https://ghcr.io \
          --repository "${{ github.repository }}-build-cache" \
          --fixed-ref kache-linux-amd64-s1-kache-0.20.0 \
          --platform linux/amd64 \
          --kache-version 0.20.0 \
          --schema-version 1 \
          --max-transfer-bytes 4294967296 \
          --max-unpacked-bytes 4294967296 \
          --max-disk-bytes 12884901888 \
          --max-files 100000 \
          --timeout-ms 600000; then
          echo "::warning::Kache snapshot restore failed; building with an empty cache."
          exit 1
        fi
        printf 'cache_id=%s\n' "${{ env.RESTORE_ID }}" >> "$GITHUB_OUTPUT"
      timeout-minutes: 15
    - id: meta
      uses: docker/metadata-action@v5
      with:
        images: ghcr.io/${{ github.repository }}
        tags: |-
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=raw,value=latest
    - id: image
      uses: docker/build-push-action@v6
      with:
        build-args: |-
          KACHE_CACHE_ID=${{ steps.restore.outputs.cache_id || env.FALLBACK_ID }}
          KACHE_MAX_SIZE=3GiB
        builder: ${{ steps.buildx.outputs.name }}
        cache-from: type=registry,ref=ghcr.io/${{ github.repository }}-build-cache:buildkit-linux-amd64-v1
        cache-to: type=registry,ref=ghcr.io/${{ github.repository }}-build-cache:buildkit-linux-amd64-v1,mode=max,ignore-error=true
        context: .
        labels: ${{ steps.meta.outputs.labels }}
        platforms: linux/amd64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
    - continue-on-error: true
      env:
        REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        REGISTRY_USERNAME: ${{ github.actor }}
      if: ${{ steps.image.outcome == 'success' }}
      name: Publish kache snapshot
      run: |-
        set -euo pipefail
        if ! node scripts/docker-cache.mjs publish \
          --cache-id "${{ steps.restore.outputs.cache_id || env.FALLBACK_ID }}" \
          --builder "${{ steps.buildx.outputs.name }}" \
          --cache-target /var/cache/kache \
          --transfer-image docker.io/library/debian:trixie-slim@sha256:abc9cb88a5587630d7f915f47b23b0668fe250fbfc6457aa4d52b534c1bbf73f \
          --registry https://ghcr.io \
          --repository "${{ github.repository }}-build-cache" \
          --fixed-ref kache-linux-amd64-s1-kache-0.20.0 \
          --platform linux/amd64 \
          --kache-version 0.20.0 \
          --schema-version 1 \
          --max-transfer-bytes 4294967296 \
          --max-unpacked-bytes 4294967296 \
          --max-disk-bytes 12884901888 \
          --max-files 100000 \
          --timeout-ms 600000 \
          --producer-repository "${{ github.repository }}" \
          --run-id "${{ github.run_id }}" \
          --run-attempt "${{ github.run_attempt }}" \
          --source-sha "${{ github.sha }}"; then
          echo "::warning::Kache snapshot publication failed; preserving the published runtime image and prior snapshot."
          exit 1
        fi
      timeout-minutes: 15
name: Docker Build & Push
on:
  push:
    tags:
    - v*
permissions:
  contents: read
  packages: write
